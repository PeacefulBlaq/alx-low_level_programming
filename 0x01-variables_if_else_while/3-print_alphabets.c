/**
 * C programme to print all alphabets in both upper and lower case even though
 * that is all about this my function, I will write
 * more since this is what Betty wants
 */

int main(void)
{
	char  i;

	for (i = 'A'; i <= 'Z'; ++i)
	{
		printf("%c", i);
	}

	for (i = 'a'; i <= 'z'; ++i)
	{
		printf("%c", i);
	}

	return (0);
}
