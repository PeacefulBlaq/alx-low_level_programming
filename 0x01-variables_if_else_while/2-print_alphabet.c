#include <stdio.h>

/**
 * This function prints all alphabets in lowercase
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; ++letter)
	{
		printf("%c", letter);
	}
printf("\n");
return (0);
}
